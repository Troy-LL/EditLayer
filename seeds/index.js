import { demoPageConfig } from "./demo.js";
import { mcpMarketplaceConfig } from "./mcpMarketplace.js";

export const PRESETS = {
  demo: {
    label: "Demo",
    config: demoPageConfig,
  },
  marketplace: {
    label: "MCP Marketplace",
    config: mcpMarketplaceConfig,
  },
};
